# Username Check App ✅

A simple React app that demonstrates **controlled inputs** and **conditional rendering**.

## 🚀 Features
- User types a username into an input field
- Live validation while typing
- Error message if the username is too short
- Success message when the username is valid
- No `useEffect` needed — re-render happens automatically on state change

## 🧠 Concepts Used
- `useState`
- Controlled input (`value` + `onChange`)
- Conditional rendering with `&&`
- Basic validation logic
- Clean separation of logic and JSX

## 📂 Project Structure
- `App.js` – main React component and logic
- `styles.css` – basic styling

## 🎯 Why this matters
This pattern is commonly used in real-world applications for:
- form validation
- user feedback
- improving UX while typing

  https://codesandbox.io/p/sandbox/cscp5d?file=%2Fsrc%2FApp.js
